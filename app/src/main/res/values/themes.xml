<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Skeleton" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="mapSkeletonStyle">@style/my_MapSkeleton</item>
    </style>
    <style name="Theme.Skeleton" parent="Base.Theme.Skeleton" />
</resources>